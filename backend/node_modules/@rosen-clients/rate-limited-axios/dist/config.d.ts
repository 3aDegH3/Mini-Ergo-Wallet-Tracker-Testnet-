import { AbstractLogger } from '@rosen-bridge/abstract-logger';
import { Rule } from './types';
export declare class RateLimitedAxiosConfig {
    protected static limitRules: Rule[];
    protected static logger: AbstractLogger;
    /**
     * Add new rule to apply rate limit for it
     * @param pattern
     * @param rateLimit
     * @param throttleWindow
     */
    static addRule(pattern: string, rateLimit: number, throttleWindow: number, timeout: number): void;
    /**
     * Remove rule from rate limited pattern list
     * @param pattern
     */
    static removeRule(pattern: string): void;
    /**
     * Get current rate limit rules
     * @returns
     */
    static getRules(): Rule[];
    /**
     * Set logger for RateLimitedAxios
     * @param logger
     */
    static setLogger(logger: AbstractLogger): void;
    /**
     * get logger of RateLimitedAxios
     * @returns
     */
    static getLogger(): AbstractLogger;
}
//# sourceMappingURL=config.d.ts.map